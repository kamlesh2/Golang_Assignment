module default {
    type Resume {
        required property fname -> str;
        property lname -> str;
        property dob -> str;
        property phone -> str;
        property email -> str;
        property file -> str;
    }
}
